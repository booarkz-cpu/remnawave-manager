services:
  cabinet:
    build: .
    container_name: remnawave-cabinet
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:3050:3050"
    volumes:
      - ./data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
