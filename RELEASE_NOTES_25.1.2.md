# v25.1.2-prod

Исправляющий релиз после дополнительного статического аудита.

Главное изменение — надёжный backup/restore PostgreSQL, включая `.age` backup, без хранения отдельного plaintext SQL dump рядом с архивами.

Статические проверки пройдены. Реальный VDS runtime test обязателен.

