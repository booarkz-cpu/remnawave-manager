# v1.4.4

После выбора ноды в пункте 25 нельзя было вернуться к списку: `[0]` выходил в главное меню, а UUID оставался в сессии и список больше не показывался.

1.4.4: список нод ↔ транспорты. `[0]` — к списку, `q` — главное меню. После add/remove остаётесь на той же ноде.

```bash
bash remnawave-manager.sh self-update
bash remnawave-manager.sh --version
# remnawave-manager 1.4.4
```

SHA256: `c94a44bf4c9e5930593bfe2227b14fed1e495efd72d300ba857aa10778d37a0d`
