# v25.2.5-prod

Internal squad is **CorgiLusi** (not AUTO). Each node gets its own config profile (`CorgiLusi`, `CorgiLusi-EDGE`, …). **Default-Profile** is deleted from internal squads and as a config profile right after bind.

```bash
curl -fL --retry 5 --retry-all-errors \
  https://cdn.jsdelivr.net/gh/booarkz-cpu/remnawave-manager@v25.2.5-prod/remnawave-manager.sh \
  -o remnawave-manager.sh
sudo bash remnawave-manager.sh --lang ru
```

Already installed: menu **4** or `sudo bash remnawave-manager.sh protocols`.

SHA256: `d428f8fac02c9904a8dec07b585fc8f0ecafd561482b1a72c8be756c101e65e2`
