# v25.1.12-prod

Исправление 502 на панели и странице подписки, маскировка SNI под сайт о корги.

- nginx шлёт официальные reverse-proxy заголовки Remnawave (без Ubuntu `proxy_params`);
- SelfSteal по умолчанию — Corgi Lusi;
- `repair` для уже установленного 25.1.11 без переустановки;
- SIGPIPE в определении SSH-порта больше не пишет ложные ошибки в лог.

Статические проверки пройдены.
